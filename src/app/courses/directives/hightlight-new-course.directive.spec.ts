import { HightlightNewCourseDirective } from './hightlight-new-course.directive';

describe('HightlightNewCourseDirective', () => {
  it('should create an instance', () => {
    const directive = new HightlightNewCourseDirective();
    expect(directive).toBeTruthy();
  });
});
